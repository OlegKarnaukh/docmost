# ===========================================
# Outline Railway Deployment Configuration
# ===========================================

# Generate with: openssl rand -hex 32
SECRET_KEY=your_secret_key_here
UTILS_SECRET=your_utils_secret_here

# Application URL (Railway domain or custom)
URL=https://your-outline.up.railway.app

# ===========================================
# Database (Railway provides these)
# ===========================================
DATABASE_URL=${{Postgres.DATABASE_URL}}
REDIS_URL=${{Redis.REDIS_URL}}

# ===========================================
# File Storage (using local for simplicity)
# ===========================================
FILE_STORAGE=local
FILE_STORAGE_LOCAL_ROOT_DIR=/var/lib/outline/data

# For S3 storage (optional, recommended for production):
# FILE_STORAGE=s3
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=us-east-1
# AWS_S3_UPLOAD_BUCKET_NAME=your-bucket
# AWS_S3_UPLOAD_BUCKET_URL=https://s3.us-east-1.amazonaws.com
# AWS_S3_FORCE_PATH_STYLE=true
# AWS_S3_ACL=private

# ===========================================
# Authentication (choose ONE provider)
# ===========================================

# --- Option 1: Google OAuth (recommended) ---
# Create at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# --- Option 2: Slack OAuth ---
# Create at: https://api.slack.com/apps
# SLACK_CLIENT_ID=your_slack_client_id
# SLACK_CLIENT_SECRET=your_slack_client_secret

# --- Option 3: Generic OIDC ---
# OIDC_CLIENT_ID=your_oidc_client_id
# OIDC_CLIENT_SECRET=your_oidc_client_secret
# OIDC_AUTH_URI=https://your-provider.com/authorize
# OIDC_TOKEN_URI=https://your-provider.com/token
# OIDC_USERINFO_URI=https://your-provider.com/userinfo
# OIDC_DISPLAY_NAME=SSO Login

# ===========================================
# Optional Settings
# ===========================================

# Allow public sharing of documents
ENABLE_UPDATES=true
FORCE_HTTPS=true
PGSSLMODE=disable

# Rate limiting
RATE_LIMITER_ENABLED=true
RATE_LIMITER_REQUESTS=1000
RATE_LIMITER_DURATION_WINDOW=60

# Default UI language
DEFAULT_LANGUAGE=en_US

# SMTP for email notifications (optional)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=your_username
# SMTP_PASSWORD=your_password
# SMTP_FROM_EMAIL=outline@example.com
# SMTP_REPLY_EMAIL=support@example.com
