# Railway Dockerfile for Docmost
# Uses the official Docmost image
FROM docmost/docmost:latest

# Railway requires PORT environment variable
ENV PORT=3000

EXPOSE 3000

# Use the original entrypoint from docmost image (don't override CMD)
